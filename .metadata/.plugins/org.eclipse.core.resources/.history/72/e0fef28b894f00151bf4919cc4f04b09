package maze;

import maze.Maze.Direction;

public class Node {
	
	static Maze maze;
	int pathCost;
	Vertex nodeVertex; //Vertex which is represented by this node
	Node parent; //Pointer to parent node
	Direction parentDirecti
	
	on;
	
	public Node(Vertex v, Node p, Direction parentD, int c){
		nodeVertex = v;
		parent = p;
		parentDirection = parentD;
		pathCost = c;
	}
	
	public Vertex expandUp(Vertex v){
		if(maze.canMoveUp(this)){
			return new Vertex(this.x, this.y - 1);
		}
		else
			return null;
	}
	
	public Vertex expandDown(Vertex v){
		if(maze.canMoveDown(this)){
			return new Vertex(this.x, this.y + 1, maze);
		}
		else
			return null;
	}
	
	public Vertex expandLeft(Vertex v){
		if(maze.canMoveLeft(this)){
			return new Vertex(this.x - 1, this.y, maze);
		}
		else
			return null;
	}
	
	public Vertex expandRight(Vertex v){
		if(maze.canMoveRight(this)){
			return new Vertex(this.x + 1, this.y, maze);
		}
		else
			return null;
	}
}
